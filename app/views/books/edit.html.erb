<%= render 'users/list'%>

<h2>Editing Book</h2>
<%= form_for(@book) do |f|%>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Option</h4>
<%= f.text_area :option %>
<%= f.submit 'Update Book' %>
<% end %>

<%= link_to 'Show', book_path(@book) %><spam>|</spam>
</MAIN>
<%= link_to 'Back', users_path %>