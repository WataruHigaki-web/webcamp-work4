

<h2>Editing Book</h2>
<%= form_for(@book) do |f|%>
<h4>title</h4>
<%= f.text_field :title %>
<h4>option</h4>
<%= f.text_area :body %>
<%= f.submit 'Update Book' %>
<% end %>

<%= link_to 'Show', book_path(@book) %><spam>|</spam>
<%= link_to 'Back', users_path %>