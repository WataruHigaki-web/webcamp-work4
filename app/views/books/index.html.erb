
<% if @book.errors.any? %>
  <div class="alert alert-warning">
    <ul>
    	<h2><%= @book.errors.count%>errors prohibited this obj from being saved:</h2>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        </ul>
        <% end %>
        
  </div>
<% end %>
<div class="row">
	<div class="col-xs-3">

<%= render 'users/list', user: @user %>
<%= render 'users/list2', book: @book %>
</div>

<div class="col-xs-9">
<h2>Books</h2>
<table class="table">
		<tr>
			<th></th>
			<th>title</th>
			<th>opinion</th>
		</tr>
	<tbody>
	<% @books.each do |book| %>
	<tr>
		<th><%= link_to (user_path(book.user)) do %>
			<%= attachment_image_tag @user, :profile_image, :fill, 50,50, fallback: "no_image.jpg" %>
			<% end %>
			</th>
		<th><%= link_to (book_path(book))do %><%= book.title %>
			<% end %>
		</th>
		<th><%= book.body %></th>
	</tr>
	<% end %>
</tbody>
</table>
</div>
</div>