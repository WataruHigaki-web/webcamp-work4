<div class='row'>

<h2>New book</h2>
 <%= form_for(book) do |f| %>

<h4>title</h4>
<%= f.text_field :title %>
<h4>opinion</h4>
<%= f.text_area :body %>

 <div class="actions row">
    <input type="submit" name="commit" value="Create Book" class="btn btn-primary col-xs-10"=Create Book />
  </div>

</div>
<% end %>